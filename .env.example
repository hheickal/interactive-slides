# Copy to .env (gitignored) and fill in. Used only by scripts/admin.mjs.
# Supabase dashboard -> Project Settings -> API.
SUPABASE_URL=https://xxxxxxxx.supabase.co

# SECRET. Bypasses Row Level Security completely. Never commit this, never put
# it in public/config.json, never send it to a browser.
SUPABASE_SERVICE_ROLE_KEY=
